import { Button } from "@/components/ui/button";
import React from "react";
import "@/styles//appButton.scss";

const InstallAppButton = () => {
  return (
    <div>
      <Button className='installAppButton'>InstallApp</Button>
    </div>
  );
};

export default InstallAppButton;
